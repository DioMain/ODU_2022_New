int function ost(int a, int b){
	type int result = a % b;
	
	return result;
}

Main {
	type string original = "COPY TEXT!";
	type string copyright = copy(original);
	
	type int offset = 7;
	
	type int nums = (ost(125, 10) + 5 - 2 / (2 * 1)) + offset;
	type int randnum = random(16);
	type int textlen = lenght("TEXT");
	
	type string con = concat(original, copyright);
	
	WriteLine copyright;
	WriteLine " ";
	WriteLine nums;
	WriteLine " ";
	WriteLine con;
	WriteLine " ";
	WriteLine randnum;
	WriteLine " ";
	WriteLine textlen;
	WriteLine " ";
	
	type string conv = intToStr(textlen);
	
	WriteLine conv;
}
